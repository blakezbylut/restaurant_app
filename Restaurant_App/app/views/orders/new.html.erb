<h2>Add a new order!</h2>

<%= form_for @order do |o| %>

  <br>
  <%= o.label :food_id %>
  <%= o.hidden_field :party_id, :value => @party.id %>
  <%= o.select( :food_id, options_for_select(@foods.map {|food| [food.name, food.cost, food.id]})) %>
  <br>

  <%= o.submit %>

<% end %>
